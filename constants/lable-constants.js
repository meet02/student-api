"use strict";

const config = require("./../config");

/*
 * Related collection name with a database & schema
 */
module.exports = {
  ADMIN_USER: "AUSR",
  ABOUT: "ABT",
  COLLECTION: "CL",
  SETTING: "SET",
};
